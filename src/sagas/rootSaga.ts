import { takeEvery } from 'redux-saga/effects';


export function* watcherSaga() {

    // watches for any actions dispatched by redux store 
    // mapp it to handler function that actually call the request:make the api calls
}